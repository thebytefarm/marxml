# marxml

> Fast markdown + XML query and mutation. Rust core, Node bindings.

This is the Node distribution of [`marxml`](https://github.com/thebytefarm/marxml). It ships a prebuilt native binary built from the [Rust crate of the same name](https://crates.io/crates/marxml).

**Status: placeholder.** `0.0.0` reserves the name. The real `0.1.0` release will ship working bindings.

See the [repository](https://github.com/thebytefarm/marxml) for status.
